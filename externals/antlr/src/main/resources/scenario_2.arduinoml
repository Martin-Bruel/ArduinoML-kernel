application dualAlarm

# Declaring bricks
sensor buttonA : 9
sensor buttonB : 10
actuator buzzer : 11

# Declaring states
on {
  buzzer <= HIGH
  buttonA is LOW and buttonB is LOW  => off
}
off {
  buzzer <= LOW
  buttonA is HIGH and buttonB is HIGH => on
}