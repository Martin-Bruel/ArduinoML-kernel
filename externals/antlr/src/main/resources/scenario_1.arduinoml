application timeSwitch

# Declaring bricks
sensor button : 9
actuator led : 12
actuator buzzer : 11

# Declaring states
on {
  led <= HIGH
  buzzer <= HIGH
  after 800 ms => off
}
off {
  led <= LOW
  buzzer <= LOW
  button is HIGH => on
}